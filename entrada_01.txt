#include <stdio.h>
int main(){
    int array[5] = {1, 2, 3, 4, 5}; 
    int even_count = 0;
    int odd_count = 0; 
    int sum = 0;
    int i, j;
    int flag = 0; 

    
    for (i = 0; i < 5; i++) {
        sum += array[i]; 

        
        if ( (array[i] % 2 == 0) && (sum > 10 || (sum <= 300) ) ) {
            even_count++; 
            flag = 1; 
        } else {
            odd_count++; 
            flag = 2; 
        }

        
        for (j = 0; j < array[i]; j++) {
            
            flag++;
	    while(j > 1){
		float nada = 12.34;
		}
        }
    }

   
    while (sum > 0) {
        sum--; 
        flag = 3; 
    }

   
    do {
        sum--;
        flag = 4; 
    } while (sum > -5); 

    
    if (sum == -5) {

        flag = 5; 
    } else {
       
        flag = 6; 
    }

   

}
